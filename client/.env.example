# Optional. When the web app is on Vercel and the API is elsewhere, set this to the API origin (no trailing slash).
# Example: VITE_API_BASE_URL=https://dev-claw-api.onrender.com
# Leave unset for local dev (Vite proxies /api to the server).
